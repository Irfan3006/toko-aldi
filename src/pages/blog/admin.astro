---
import BlogLayout from "../../layouts/BlogLayout.astro";
---
<BlogLayout noindex={true}>
  <Fragment slot="head">
    <title>Admin Dashboard - Sibra Tech Blog</title>
    <meta name="robots" content="noindex, nofollow" />
    <style is:global>
      .admin-card {
        border-left: 4px solid var(--accent);
      }
      .table,
      .table tr,
      .table td {
        background-color: transparent !important;
        color: var(--paragraph) !important;
        box-shadow: none !important;
      }
      .table {
        margin-bottom: 0;
      }
      .table thead th {
        background: rgba(255, 255, 255, 0.04) !important;
        color: var(--headline) !important;
        border: none;
        font-weight: 600;
        padding: 1.2rem 1rem;
        box-shadow: none !important;
      }
      .table td {
        border-color: rgba(255, 255, 255, 0.05) !important;
        padding: 1.2rem 1rem;
      }
      .table-hover tbody tr:hover {
        background-color: rgba(255, 255, 255, 0.02) !important;
        color: var(--headline);
      }
      .table-hover tbody tr:hover td {
        background-color: transparent !important;
        color: inherit !important;
      }
      .badge-admin {
        background: rgba(239, 68, 68, 0.1) !important;
        color: #ef4444 !important;
        border: 1px solid rgba(239, 68, 68, 0.2);
      }
      .badge-user {
        background: rgba(37, 99, 235, 0.1) !important;
        color: var(--accent) !important;
        border: 1px solid rgba(37, 99, 235, 0.2);
      }
    </style>
  </Fragment>

  <div class="container-fluid my-5 py-5 px-4" id="admin-dashboard-container" style="display: none;">
    <div class="row">
      <div class="col-md-3 col-lg-2 mb-4">
        <div class="card shadow-sm border-0 h-100">
          <div class="card-body p-0">
            <div class="list-group list-group-flush border-0">
              <a href="#dashboard" class="list-group-item list-group-item-action active border-0 rounded-0 py-3" data-bs-toggle="pill">
                <i class="bi bi-speedometer2 me-2"></i> Dashboard
              </a>
              <a href="#users" class="list-group-item list-group-item-action border-0 py-3" data-bs-toggle="pill">
                <i class="bi bi-people me-2"></i> Pengguna
              </a>
              <a href="#posts" class="list-group-item list-group-item-action border-0 py-3" data-bs-toggle="pill">
                <i class="bi bi-file-text me-2"></i> Postingan
              </a>
            </div>
          </div>
        </div>
      </div>

      <div class="col-md-9 col-lg-10">
        <h2 class="fw-bold mb-4">Admin Dashboard</h2>
        <div class="tab-content">
          <div class="tab-pane fade show active" id="dashboard">
            <div class="row g-4 mb-4">
              <div class="col-sm-6 col-xl-3">
                <div class="card admin-card shadow-sm border-0">
                  <div class="card-body">
                    <div class="d-flex align-items-center">
                      <div class="flex-shrink-0 bg-primary bg-opacity-10 p-3 rounded">
                        <i class="bi bi-people fs-3 text-primary"></i>
                      </div>
                      <div class="flex-grow-1 ms-3">
                        <p class="text-muted mb-0">Total Pengguna</p>
                        <h3 class="mb-0 fw-bold" id="stat-users">-</h3>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="col-sm-6 col-xl-3">
                <div class="card admin-card shadow-sm border-0" style="border-left-color: #10b981;">
                  <div class="card-body">
                    <div class="d-flex align-items-center">
                      <div class="flex-shrink-0 bg-success bg-opacity-10 p-3 rounded">
                        <i class="bi bi-file-earmark-text fs-3 text-success"></i>
                      </div>
                      <div class="flex-grow-1 ms-3">
                        <p class="text-muted mb-0">Total Postingan</p>
                        <h3 class="mb-0 fw-bold" id="stat-posts">-</h3>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="col-sm-6 col-xl-3">
                <div class="card admin-card shadow-sm border-0" style="border-left-color: #f59e0b;">
                  <div class="card-body">
                    <div class="d-flex align-items-center">
                      <div class="flex-shrink-0 bg-warning bg-opacity-10 p-3 rounded">
                        <i class="bi bi-shield-slash fs-3 text-warning"></i>
                      </div>
                      <div class="flex-grow-1 ms-3">
                        <p class="text-muted mb-0">Moderasi Spam</p>
                        <button class="btn btn-sm btn-warning mt-1 fw-bold text-white px-3" id="btn-cleanup-spam" style="border-radius: 8px;">
                          Scan & Bersihkan
                        </button>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <div class="card shadow-sm border-0">
              <div class="card-body">
                <h5 class="card-title fw-bold">Selamat Datang di Panel Admin</h5>
                <p class="text-muted">Kelola konten postingan siber, awasi data statistik pembaca, dan jalankan pemindaian spam otomatis dari dashboard ini.</p>
              </div>
            </div>
          </div>

          <div class="tab-pane fade" id="users">
            <div class="card shadow-sm border-0">
              <div class="card-body">
                <h5 class="card-title fw-bold mb-4">Kelola Pengguna</h5>
                <div class="table-responsive">
                  <table class="table table-hover align-middle">
                    <thead>
                      <tr>
                        <th>Avatar</th>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Joined</th>
                        <th>Actions</th>
                      </tr>
                    </thead>
                    <tbody id="users-table-body">
                      <tr><td colspan="6" class="text-center">Loading...</td></tr>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>

          <div class="tab-pane fade" id="posts">
            <div class="card shadow-sm border-0">
              <div class="card-body">
                <h5 class="card-title fw-bold mb-4">Moderasi Postingan</h5>
                <div class="table-responsive">
                  <table class="table table-hover align-middle">
                    <thead>
                      <tr>
                        <th>Title</th>
                        <th>Author</th>
                        <th>Category</th>
                        <th>Views</th>
                        <th>Date</th>
                        <th>Actions</th>
                      </tr>
                    </thead>
                    <tbody id="posts-table-body">
                      <tr><td colspan="6" class="text-center">Loading...</td></tr>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <Fragment slot="scripts">
    <script is:inline>
      document.addEventListener("DOMContentLoaded", async () => {
        Auth.requireAdmin();
        const user = Auth.getSession();
        if (user && user.role === "admin") {
          document.getElementById("admin-dashboard-container").style.display = "block";
        }
        let users = [];
        let posts = [];

        async function loadData() {
          const [usersRes, postsRes] = await Promise.all([API.getUsers(), API.getPosts()]);
          if (usersRes.success) {
            users = usersRes.data;
            document.getElementById("stat-users").innerText = users.length;
            renderUsers(users);
          }
          if (postsRes.success) {
            posts = postsRes.data;
            document.getElementById("stat-posts").innerText = posts.length;
            renderPosts(posts);
          }
        }

        function renderUsers(usersList) {
          const tbody = document.getElementById("users-table-body");
          if (usersList.length === 0) {
            tbody.innerHTML = '<tr><td colspan="6" class="text-center">Pengguna tidak ditemukan</td></tr>';
            return;
          }
          tbody.innerHTML = usersList
            .map((u) => {
              const safeUsername = escapeHTML(u.username);
              const safeEmail = escapeHTML(u.email);
              const safeAvatar = escapeHTML(u.avatar || "");
              const safeRole = escapeHTML(u.role);
              return `
                <tr>
                  <td><img src="${safeAvatar}" class="rounded-circle-img" style="width:40px;height:40px;border: 1px solid var(--stroke);" referrerpolicy="no-referrer"></td>
                  <td class="fw-600 text-headline">${safeUsername}</td>
                  <td style="color: var(--paragraph); opacity: 0.8;">${safeEmail}</td>
                  <td><span class="badge ${u.role === "admin" ? "badge-admin" : "badge-user"} text-uppercase" style="font-size: 0.7rem; letter-spacing: 1px;">${safeRole}</span></td>
                  <td style="color: var(--paragraph); opacity: 0.6; font-size:0.85rem;">${formatDate(u.created_at)}</td>
                  <td>
                    ${u.id !== user.id ? `<button class="btn btn-sm btn-link text-danger p-0" onclick="deleteUser('${escapeHTML(u.id)}')" title="Hapus Pengguna"><i class="bi bi-trash3 fs-5"></i></button>` : '<span class="badge bg-secondary bg-opacity-10 text-muted">YOU</span>'}
                  </td>
                </tr>
              `;
            })
            .join("");
        }

        function renderPosts(postsList) {
          const tbody = document.getElementById("posts-table-body");
          if (postsList.length === 0) {
            tbody.innerHTML = '<tr><td colspan="6" class="text-center">Postingan tidak ditemukan</td></tr>';
            return;
          }
          tbody.innerHTML = postsList
            .map((p) => {
              const safeTitle = escapeHTML(p.title);
              const safeAuthor = escapeHTML(p.author_name);
              const safeCategory = escapeHTML(p.category);
              const safeSlug = encodeURIComponent(p.slug);
              const safeViews = p.views || 0;
              return `
                <tr>
                  <td class="fw-600"><a href="/blog/post/${safeSlug}" target="_blank" rel="noopener noreferrer" class="text-headline text-decoration-none hover-accent">${safeTitle}</a></td>
                  <td style="color: var(--paragraph);">${safeAuthor}</td>
                  <td><span class="badge badge-user">${safeCategory}</span></td>
                  <td style="color: var(--paragraph); font-size:0.85rem;"><i class="bi bi-eye me-1 text-primary"></i>${safeViews}</td>
                  <td style="color: var(--paragraph); opacity: 0.6; font-size:0.85rem;">${formatDate(p.created_at)}</td>
                  <td>
                    <div class="d-flex gap-2">
                      <a href="/blog/post/${safeSlug}" target="_blank" rel="noopener noreferrer" class="btn btn-sm btn-link text-info p-0" title="Lihat Postingan"><i class="bi bi-eye fs-5"></i></a>
                      <a href="/blog/edit-post?id=${encodeURIComponent(p.id)}" class="btn btn-sm btn-link text-primary p-0" title="Edit"><i class="bi bi-pencil-square fs-5"></i></a>
                      <button class="btn btn-sm btn-link text-danger p-0" onclick="deletePost('${escapeHTML(p.id)}')" title="Hapus"><i class="bi bi-trash3 fs-5"></i></button>
                    </div>
                  </td>
                </tr>
              `;
            })
            .join("");
        }

        window.deleteUser = async (id) => {
          const result = await Swal.fire({
            title: "Apakah Anda yakin?",
            text: "Ini juga akan menghapus semua postingan dari pengguna ini!",
            icon: "warning",
            showCancelButton: true,
            confirmButtonColor: "#dc3545",
            confirmButtonText: "Ya, hapus pengguna",
            cancelButtonText: "Batal",
          });
          if (result.isConfirmed) {
            const res = await API.deleteUser(id);
            if (res.success) {
              showToast("success", "Pengguna berhasil dihapus");
              loadData();
            } else {
              showToast("error", "Error", res.message);
            }
          }
        };

        window.deletePost = async (id) => {
          const result = await Swal.fire({
            title: "Apakah Anda yakin?",
            text: "Anda akan menghapus postingan ini.",
            icon: "warning",
            showCancelButton: true,
            confirmButtonColor: "#dc3545",
            confirmButtonText: "Ya, hapus postingan",
            cancelButtonText: "Batal",
          });
          if (result.isConfirmed) {
            const res = await API.deletePost(id);
            if (res.success) {
              showToast("success", "Postingan berhasil dihapus");
              loadData();
            } else {
              showToast("error", "Error", res.message);
            }
          }
        };

        const btnCleanup = document.getElementById("btn-cleanup-spam");
        if (btnCleanup) {
          btnCleanup.addEventListener("click", async () => {
            const result = await Swal.fire({
              title: "Pindai & Bersihkan Spam?",
              text: "Sistem akan memindai seluruh postingan di Google Sheets dan menghapus secara otomatis semua postingan yang terdeteksi sebagai spam.",
              icon: "question",
              showCancelButton: true,
              confirmButtonColor: "#f59e0b",
              confirmButtonText: "Ya, pindai sekarang",
              cancelButtonText: "Batal",
            });
            if (result.isConfirmed) {
              btnCleanup.disabled = true;
              btnCleanup.innerHTML = `<span class="spinner-border spinner-border-sm me-1" role="status" aria-hidden="true"></span> Memindai...`;
              
              const res = await API.cleanupSpam();
              
              btnCleanup.disabled = false;
              btnCleanup.innerHTML = "Scan & Bersihkan";
              
              if (res.success) {
                if (res.count > 0) {
                  const titles = res.deleted.map(p => `<li>${escapeHTML(p.title)}</li>`).join('');
                  await Swal.fire({
                    title: `Pembersihan Selesai!`,
                    html: `<p>Berhasil menghapus <strong>${res.count}</strong> postingan spam:</p><ul class="text-start" style="max-height:200px; overflow-y:auto; font-size:0.9rem; color: var(--paragraph);">${titles}</ul>`,
                    icon: "success"
                  });
                } else {
                  showToast("success", "Tidak ada postingan spam yang ditemukan.");
                }
                loadData();
              } else {
                showToast("error", "Error", res.message || "Gagal melakukan pembersihan spam.");
              }
            }
          });
        }

        const triggerTabList = [].slice.call(document.querySelectorAll(".list-group-item"));
        triggerTabList.forEach(function (triggerEl) {
          triggerEl.addEventListener("click", function (e) {
            e.preventDefault();
            
            // Handle Bootstrap tabs manually to prevent router reload issues
            triggerTabList.forEach((el) => el.classList.remove("active"));
            this.classList.add("active");
            
            const targetId = this.getAttribute("href").substring(1);
            document.querySelectorAll(".tab-pane").forEach((pane) => {
              pane.classList.remove("show", "active");
              if (pane.id === targetId) {
                pane.classList.add("show", "active");
              }
            });
          });
        });
        loadData();
      });
    </script>
  </Fragment>
</BlogLayout>
